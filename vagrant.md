For doing a snapshot in vagrant box:

List
```bash
vagrant snapshot list <vm_name>
```

Save
```bash
vagrant snapshot save <vm_name> <name>
```

Restore
```bash
vagrant snapshot restore <vm_name> <name>
```
